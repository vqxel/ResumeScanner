# Python dependencies for ATS Resume Parser
# Install with: pip install -r requirements.txt

pyresparser>=1.0.6
spacy>=3.0.0

# Note: After installing, run this to download the spaCy model:
# python -m spacy download en_core_web_sm
